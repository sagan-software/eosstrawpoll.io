open TypedGlamor

let main = css [
    display flex;
    flexDirection rowReverse;
]

let form = css [
    flex_ auto;
]

let sidebar = css [
    flexShrink 0.;
    width (px 250)
]

let poll = css []

let pollTitle = css []

let optionList = css [
    display flex;
    flexDirection column;
]

let option = css []

let optionInput = css []
